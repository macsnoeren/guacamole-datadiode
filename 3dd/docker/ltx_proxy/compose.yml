services:
  ltx_proxy:
    build:
      context: ../../
      dockerfile: docker/ltx_proxy/Dockerfile
    stop_grace_period: 1s
    container_name: ltx_proxy
    restart: unless-stopped
    network_mode: "host"
    command:
      [
        "./build/ltx_proxy",
        "5502",
        "10.0.1.2", "5503"
      ]
