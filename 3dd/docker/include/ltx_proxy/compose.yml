include:
  - ../gmwebserver/compose.yml
services:
  gmlbroker:
    build:
      context: ../../
      dockerfile: docker/include/ltx_proxy/Dockerfile
    stop_grace_period: 1s
    container_name: gmlbroker
    restart: unless-stopped
    network_mode: "host"
    command:
      [
        "./build/gmlbroker",
        "0.0.0.0", "4823",
        "10.0.4.2", "5505",
        "10.0.0.2", "5500"
      ]
